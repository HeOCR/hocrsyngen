# Current System State

- Active branch: `main`
- Last completed roadmap item: `S8h` — Wet-test report, open PR. `S8g` — LLM
  triage packet export, squash-merged in PR #65. `S8f` - human review sidecar,
  squash-merged in PR #63. `S8e` - deterministic warning metrics was
  squash-merged in PR #61. `S8d` - human-first static gallery was squash-merged
  in PR #60. `S8c` - candidate evidence-run handoff was squash-merged in PR #59.
  `S8b` - deterministic wet-test smoke run artifact generator was squash-merged
  in PR #58.
- Next planned item: `S8i` — Regression promotion and schema/CI decision:
  promote confirmed wet-test findings into small deterministic automated tests,
  and only then decide whether wet-test artifact schemas or a CI smoke profile
  are stable enough to adopt.
- Current blockers: none known.

## Active Task Breakdown

S8h is complete in an open PR. Added `src/hocrsyngen/wet_report.py`, the
`wet-report` CLI command, and `tests/test_wet_report.py`.

When starting S8i:

- [ ] open branch `feature/s8i-regression-promotion`
- [ ] design regression promotion workflow
- [ ] implement regression-promotion path
- [ ] add tests
- [ ] run validation checklist
- [ ] open PR

## Validation Checklist

```bash
. .venv/bin/activate && python -m pip install -e ".[test]"
. .venv/bin/activate && python -m pytest
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli templates
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli contracts --format json
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli generate --count 2 --seed 17 --output out/fixture-batch
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli validate out/fixture-batch --format json
git diff --check
```

Use `.venv` if bare `python` is unavailable in the current shell.

## Context Pointers

- [README.md](README.md)
- [AGENTS.md](AGENTS.md)
- [llms.txt](llms.txt)
- [docs/roadmap.md](docs/roadmap.md)
- [docs/production_readiness.md](docs/production_readiness.md)
- [docs/wet_testing_program_plan.md](docs/wet_testing_program_plan.md)
