# Current System State

- Active branch: `test/s8c-human-first-static-gallery`
- Active PR target: `S8d: Human-first static gallery`
- Active roadmap item: `S8d` - dependency-light static HTML gallery over an
  existing wet-test run for human inspection of generated pages, logical Hebrew
  text, and public manifest metadata.
- Last completed roadmap item: `S8c` - candidate evidence-run handoff,
  squash-merged in PR #59. `S8b` - deterministic wet-test smoke run artifact
  generator - was squash-merged in PR #58.
- Current blockers: none known.
- This PR should add only the static gallery CLI and planning alignment. It must
  not add `hocrgen` imports, adapter behavior, release governance, review
  sidecars, warning metrics, LLM triage packets, wet-test reports, regression
  promotion, schemas, network/LLM/GPU dependencies, Arabic support, validation
  semantic changes, or `generation_manifest.json` v1 changes.

## Active Task Breakdown

- [x] start implementation branch from latest `main`
- [x] inspect AGENTS, docs index, production-readiness boundaries, roadmap, and
  wet-testing plan
- [x] add `wet-gallery` CLI over existing `wet-run` artifacts
- [x] render static HTML with relative image links, escaped logical Hebrew text,
  and public sample/page/template/style/condition/degradation/font metadata
- [x] add focused tests plus installed-package CLI smoke coverage
- [x] update README, docs index, roadmap, production-readiness, wet-testing,
  testing-quality, and llms state
- [x] run required local validation commands
- [x] run S8b wet-run and new S8d gallery command, then inspect generated HTML
- [ ] push branch
- [ ] open non-draft PR with labels and milestone

## Validation Checklist

```bash
. .venv/bin/activate && python -m pip install -e ".[test]"
. .venv/bin/activate && python -m pytest
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli templates
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli contracts --format json
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli generate --count 2 --seed 17 --output out/fixture-batch
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli validate out/fixture-batch --format json
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli wet-run --profile smoke --seed 17 --output out/wet-tests/smoke-17 --format json
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli wet-gallery out/wet-tests/smoke-17 --output out/wet-tests/smoke-17/gallery --format json
git diff --check
```

Use `.venv` if bare `python` is unavailable in the current shell.

## PR Handoff Notes

- PR title: `S8d: Human-first static gallery`
- Commit message: `S8d: add human-first wet-test gallery`
- Labels: `type:feature`, `area:cli`, `area:testing`, `area:planning`,
  `phase:s8`, `priority:p2`, and `size:medium`.
- Milestone: `Phase S8 — Wet Testing And Generator-Quality Evidence`. Create it
  if it is missing before updating the PR.
- Use GitHub MCP/app tools first for PR creation and metadata updates. Use CLI
  only for unsupported local git operations or unsupported GitHub metadata
  operations.
- After S8d merges, the next planned implementation slice is `S8e` -
  deterministic warning metrics. Do not start S8e in the S8d PR.

## Context Pointers

- [README.md](README.md)
- [AGENTS.md](AGENTS.md)
- [llms.txt](llms.txt)
- [docs/README.md](docs/README.md)
- [docs/production_readiness.md](docs/production_readiness.md)
- [docs/roadmap.md](docs/roadmap.md)
- [docs/testing_and_quality.md](docs/testing_and_quality.md)
- [docs/wet_testing_program_plan.md](docs/wet_testing_program_plan.md)
