# Current System State

- Active branch: `feature/s4d-style-consistency-checks`
- Active PR target: `S4d: Add style consistency checks`
- Active PR: not opened yet.
- Active roadmap item: `S4d` - Style consistency checks.
- Last completed PR: #37 - `S4c: Add rendering-control condition bundles`,
  squash-merged to `main`.
- Current blockers: none known.
- This PR should add focused automated coverage proving deterministic synthetic
  style controls remain reproducible across a batch and distinguishable across
  supported style bundles, without changing manifest v1, CLI output, packaged
  fixtures, dependencies, validation behavior, release eligibility, or
  publication behavior.

## Active Task Breakdown

- [x] start S4d feature branch from latest `main` after PR #37
- [x] inspect roadmap, ADR 0005, testing docs, and current S4b/S4c generator and tests
- [x] add batch-level deterministic style consistency profile helpers
- [x] add fixed-seed repeatability coverage for all supported style bundles
- [x] add distinguishability coverage across supported style bundles
- [x] update S4d planning and testing docs
- [x] run required local validation commands
- [ ] stage/commit
- [ ] push branch
- [ ] open non-draft S4d PR with labels and Phase S4 milestone

## Validation Checklist

```bash
. .venv/bin/activate && python -m pip install -e ".[test]"
. .venv/bin/activate && python -m pytest
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli templates
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli contracts --format json
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli generate --count 2 --seed 17 --output out/fixture-batch
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli validate out/fixture-batch --format json
git diff --check
```

Use `.venv` if bare `python` is not available or does not have the declared test dependencies.

## PR Handoff Notes

- PR title: `S4d: Add style consistency checks`
- Commit message: `S4d: Add style consistency checks`
- Labels: `type:test`, `area:generator`, `area:docs`, `phase:s4`, `size:small`
- Milestone: `Phase S4 — Persona/Style/Condition Controls`
- Use GitHub MCP/app tools first for PR creation and metadata updates. Use CLI
  only for unsupported local git operations or unsupported GitHub metadata operations.

## Context Pointers

- [README.md](README.md)
- [AGENTS.md](AGENTS.md)
- [llms.txt](llms.txt)
- [docs/README.md](docs/README.md)
- [docs/architecture.md](docs/architecture.md)
- [docs/generation_manifest_v1.md](docs/generation_manifest_v1.md)
- [docs/hocrgen_integration.md](docs/hocrgen_integration.md)
- [docs/decisions/0005-persona-style-condition-semantics.md](docs/decisions/0005-persona-style-condition-semantics.md)
- [docs/roadmap.md](docs/roadmap.md)
- [docs/testing_and_quality.md](docs/testing_and_quality.md)
