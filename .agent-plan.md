# Current System State

- Active branch: `feature/evidence-run-cli`
- Active PR target: `S8c: Candidate evidence-run handoff`
- Active roadmap item: `S8c` - single-command operator evidence capture for
  downstream `hocrgen` preflight runs.
- Last completed roadmap item: `S8b` - deterministic wet-test smoke run
  artifact generator, squash-merged in PR #58.
- Current blockers: none known.
- This PR should add only the candidate evidence-run CLI wrapper and planning
  alignment. It must not add `hocrgen` imports, adapter behavior, release
  governance, review sidecars, caps, export, publication, network/LLM/GPU
  dependencies, or `generation_manifest.json` v1 changes.

## Active Task Breakdown

- [x] start implementation branch from latest `main`
- [x] inspect AGENTS, docs index, production-readiness boundaries, roadmap, and
  wet-testing plan
- [x] add `evidence-run` CLI wrapper using existing public generation,
  validation, template catalog, contract fixture, and rendering coverage
  surfaces
- [x] write `candidate_evidence_run_report.v1`, `RUN_NOTES.md`, and `SHA256SUMS`
- [x] add focused tests plus installed-package and wheel CLI smoke coverage
- [x] run required local validation commands
- [x] run a larger deterministic evidence run for downstream preflight
- [x] run `hocrgen hocrsyngen-preflight --evidence-root PATH`
- [ ] push branch
- [ ] open non-draft PR with labels and milestone
- [ ] perform brutal self-review and address findings
- [ ] address applicable Copilot/review feedback if present
- [ ] wait for green CI and no unresolved review threads
- [ ] squash-merge PR

## Validation Checklist

```bash
. .venv/bin/activate && python -m pip install -e ".[test]"
. .venv/bin/activate && python -m pytest
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli templates
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli contracts --format json
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli generate --count 2 --seed 17 --output out/fixture-batch
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli validate out/fixture-batch --format json
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli evidence-run --count 80 --seed 101 --output-root /tmp/hocrsyngen-candidate-batches --run-id f6f2-seed101-count80 --overwrite --format json --color never
git diff --check
```

Use `.venv` if bare `python` is unavailable in the current shell.

## PR Handoff Notes

- PR title: `S8c: Candidate evidence-run handoff`
- Commit message: `S8c: add candidate evidence-run handoff`
- Labels: `type:feature`, `area:cli`, `area:testing`, `area:planning`,
  `phase:s8`, `priority:p2`, and `size:medium`.
- Milestone: `Phase S8 — Wet Testing And Generator-Quality Evidence`. Create it
  if it is missing before updating the PR.
- Use GitHub MCP/app tools first for PR creation and metadata updates. Use CLI
  only for unsupported local git operations or unsupported GitHub metadata
  operations.
- After S8c merges, the next planned implementation slice is `S8d` - the
  human-first static gallery. Do not start S8d in the S8c PR.

## Context Pointers

- [README.md](README.md)
- [AGENTS.md](AGENTS.md)
- [llms.txt](llms.txt)
- [docs/README.md](docs/README.md)
- [docs/production_readiness.md](docs/production_readiness.md)
- [docs/roadmap.md](docs/roadmap.md)
- [docs/testing_and_quality.md](docs/testing_and_quality.md)
- [docs/wet_testing_program_plan.md](docs/wet_testing_program_plan.md)
- [docs/rendering_coverage_reporting.md](docs/rendering_coverage_reporting.md)
- [docs/hocrgen_adapter_handoff_checklist.md](docs/hocrgen_adapter_handoff_checklist.md)
