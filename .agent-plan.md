# Current System State

- Active branch: `main`
- Last completed roadmap item: `S8g` — LLM triage packet export, squash-merged
  in PR #65. `S8f` - human review sidecar, squash-merged in PR #63. `S8e` -
  deterministic warning metrics was squash-merged in PR #61. `S8d` - human-first
  static gallery was squash-merged in PR #60. `S8c` - candidate evidence-run
  handoff was squash-merged in PR #59. `S8b` - deterministic wet-test smoke run
  artifact generator was squash-merged in PR #58.
- Next planned item: `S8h` — Wet-test report: combine run metadata, code
  warnings, human review, optional LLM notes, top examples, and follow-up
  recommendations into a developer-facing generator-quality report with explicit
  non-release wording.
- Current blockers: none known.

## Active Task Breakdown

No active implementation task. The previous session completed S8g:
`S8g: LLM triage packet export` (PR #65, open). That PR added
`src/hocrsyngen/wet_triage.py`, the `wet-llm-packet` CLI command, and
`tests/test_wet_triage.py`.

When starting S8h:

- [ ] open branch `feature/s8h-wet-test-report`
- [ ] design report schema (combine run metadata, analysis, human review, LLM notes)
- [ ] implement `wet-report` command
- [ ] add tests
- [ ] run validation checklist
- [ ] open PR

## Validation Checklist

```bash
. .venv/bin/activate && python -m pip install -e ".[test]"
. .venv/bin/activate && python -m pytest
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli templates
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli contracts --format json
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli generate --count 2 --seed 17 --output out/fixture-batch
. .venv/bin/activate && PYTHONPATH=src python -m hocrsyngen.cli validate out/fixture-batch --format json
git diff --check
```

Use `.venv` if bare `python` is unavailable in the current shell.

## Context Pointers

- [README.md](README.md)
- [AGENTS.md](AGENTS.md)
- [llms.txt](llms.txt)
- [docs/roadmap.md](docs/roadmap.md)
- [docs/production_readiness.md](docs/production_readiness.md)
- [docs/wet_testing_program_plan.md](docs/wet_testing_program_plan.md)
