# Visual Inspection Rubric

This rubric defines S3d human inspection guidance for candidate synthetic Hebrew
OCR/HTR batches generated by `hocrsyngen`. It is a reviewer checklist for
layout realism, Hebrew readability, visible artifacts, and candidate rejection.
It does not add machine-readable review status, release eligibility, balancing
policy, publication behavior, manifest fields, schema changes, fixture changes,
or generator behavior.

Generated batches remain candidate synthetic inputs. `hocrgen` remains
responsible for downstream review workflows, dataset governance, synthetic caps,
privacy, dedupe, release assembly, export, and publication.

## Review Scope

Inspect the rendered JPEG pages together with the generated manifest and public
template catalog when needed. Use stable public identifiers only:

- `template_id`
- `recipe_id`
- `layout_style`
- `font_id`
- `degradation_preset`
- sample id, page id, seed, and sample index

The rubric is intended for spot checks of governed templates and degradation
variants, especially:

- `printed_letter`
- `handwritten_note`
- `archive_card`
- `printed_letter_heavy_scan`
- `handwritten_note_heavy_wear`
- `archive_card_faded_scan`

Do not infer release readiness, real-source provenance, real identity,
authorship, medical condition, psychological condition, or sensitive attributes
from visual review. Persona, condition, stamps, identifiers, marginalia, and wear
are synthetic generator controls only.

## Pass Criteria

A candidate page is visually acceptable for S3 document-layout realism when all
of these conditions hold:

- The document family is recognizable from visible layout structure, not only
  from manifest provenance.
- Hebrew text remains readable enough for human inspection, including through
  the selected degradation preset.
- RTL visual flow looks plausible: Hebrew is not obviously reversed, broken into
  isolated glyphs, or displaced by mixed-direction fragments.
- Text, ruled areas, stamps, identifiers, marginalia, and borders do not clip,
  crop, collide, or create incoherent overlap.
- Degradation artifacts look like plausible scan or wear artifacts for the
  template family and do not dominate the document.
- Repeated regions such as form rows, ruled notebook lines, card sections, and
  footer/header areas remain visually aligned enough to inspect.
- Synthetic identifiers, dates, stamps, and labels are inspectable when they are
  part of the governed recipe.
- JPEG assets open correctly, have the expected page-like dimensions, and do
  not appear blank, fully saturated, or corrupt.

## Template-Specific Checks

For `printed_letter` and `printed_letter_heavy_scan`, reviewers should confirm:

- The page reads as a printed letter or form-like page with a stable header,
  body area, and footer or administrative text structure.
- Printed Hebrew line spacing and margins are plausible.
- Heavy scan degradation makes blur, skew, grain, or contrast shifts visible
  without hiding the main body text.
- Page borders, body text, and footer elements are not cut off by rotation,
  blur, crop, or edge artifacts.

For `handwritten_note` and `handwritten_note_heavy_wear`, reviewers should
confirm:

- The page reads as a handwritten-like notebook or note page rather than a
  printed form.
- Baseline drift, marginalia, and ruled-line behavior remain plausible and do
  not make text blocks incoherent.
- Heavy wear is visible but does not erase the Hebrew text or turn marginal
  annotations into unreadable noise.
- Ruled lines, note text, and annotations do not overlap in ways that hide the
  intended content.

For `archive_card` and `archive_card_faded_scan`, reviewers should confirm:

- The page reads as a compact multi-region archive card with bordered sections,
  header/title area, ruled body regions, identifiers, and a synthetic stamp.
- Synthetic archive identifiers, date-like labels, footer identifiers, and stamp
  text remain inspectable where present.
- Faded scan degradation looks like age, light scan loss, or contrast fading
  without destroying the card structure.
- Borders and sections remain coherent after skew, blur, fading, or grain.

## Rejection Criteria

Reject or flag a candidate page for generator-quality follow-up when any of
these are visible:

- Hebrew text is unreadable because of rendering failure, excessive blur, severe
  compression, over-darkening, over-fading, or missing ink.
- Hebrew appears visually reversed, badly shaped, split into isolated glyphs, or
  inconsistent with the expected RTL rendering path.
- Important text, borders, stamps, identifiers, ruled regions, or marginalia are
  clipped by the page boundary.
- Text blocks collide with each other, with stamps, with borders, or with ruled
  regions in a way that obscures content.
- Artifacts look synthetic in a way that breaks the document illusion, such as
  repeated mechanical noise, impossible stains, excessive uniform grain, or
  implausible skew for the family.
- The visible layout does not match the governed template family or recipe.
- The page is blank, corrupt, fully saturated, mostly empty by accident, or not a
  readable JPEG page asset.
- The visual content implies real provenance, real identity, real authorship, or
  sensitive claims instead of synthetic controls.

Use concise rejection notes that identify the visual problem and the stable
public sample/template identifiers. Do not record reviewer identity, release
eligibility, dataset acceptance, or downstream governance decisions in
`hocrsyngen` outputs.

## Generator Issues Versus Downstream Governance

Treat these as `hocrsyngen` generator-quality issues:

- unreadable Hebrew caused by rendering or excessive degradation;
- clipping, cropping, overlap, or broken layout regions;
- implausible artifacts introduced by template or degradation settings;
- governed template/catalog mismatches visible in rendered assets;
- JPEG readability or dimension problems.

Treat these as downstream `hocrgen` governance concerns, not `hocrsyngen`
baseline output behavior:

- whether a candidate enters a public dataset release;
- whether a batch satisfies release caps, balancing rules, or source-composition
  policy;
- reviewer assignment, review status, acceptance status, rejection status, or
  audit workflow state;
- privacy, dedupe, leakage checks, benchmark selection, export, and publication.

Future review sidecars may record inspection evidence outside
`generation_manifest.json` v1, but they must be explicitly designed, documented,
and tested before use. Until then, this rubric is human-readable guidance only.
